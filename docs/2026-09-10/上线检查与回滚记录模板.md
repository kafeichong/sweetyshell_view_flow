# 上线检查与回滚记录模板

## 1. 基本信息

- 任务：
- 迭代编号：Task 0
- 版本/镜像：
- 部署环境：
- 负责人：
- 开始时间：
- 完成时间：

## 2. 上线前检查（发布前）

- [ ] 数据库备份完成（执行时间、备份路径）：
- [ ] 迁移文件与数据库版本核对（migration name / checksum）：
- [ ] 部署镜像 tag 与依赖清单确认：
- [ ] 环境变量核查（仅新增配置项）：

## 3. 上线步骤

1. 拉起/更新 `video-flow-backend` 容器
2. 拉起/更新 `video-flow-worker` 容器
3. 复核 `postgres` 及网络连通
4. 确认旧接口 `/api/tasks` 请求可达
5. 查看应用日志（启动与异常关键字）

## 4. 上线后 Smoke Test（发布后）

- [ ] 只读/写基础接口可用：
  - `GET /api/tasks`
  - `POST /api/tasks`
  - `GET /api/tasks/pending`
  - `GET /api/tasks/:id`
  - `PATCH /api/tasks/:id`
- [ ] 任务创建默认 `status = pending`
- [ ] `PATCH` 到 `completed` 与 `failed` 时返回 `completedAt`
- [ ] 后端测试通过（本地执行口径）：
  - `cd packages/backend && npm test`
- [ ] Worker 契约测试通过（本地执行口径）：
  - `cd packages/worker && pytest tests/test_mvp_backend_contract.py -q`

## 5. 回退方案

- [ ] 回滚命令：
  - `git checkout` / 镜像切换到上一个版本：
  - `docker-compose -f docker-compose.yml down`
  - `docker-compose -f docker-compose.yml up -d`
- [ ] 回滚验证项：
  - [ ] 旧 `/api/tasks` 可用
  - [ ] 数据库未异常新增字段（如无新增字段则无需回退）
  - [ ] 关键任务链路可继续读写

## 6. 风险与跟进

- 运行风险：
- 观察点（任务延迟、回写失败率、错误码）：
- 跟进人：
- 结项确认：

