# 服务器环境检查报告

> 服务器 IP：8.140.49.56  
> 检查时间：2026-09-09 16:46

---

## ✅ 服务器基本信息

### 系统信息
- **操作系统**：Ubuntu 22.04.5 LTS
- **内核版本**：5.15.0-177-generic
- **架构**：x86_64

### 硬件资源
- **CPU 核心数**：4 核
- **内存**：14GB（使用 25%，剩余 12GB 可用）✅
- **磁盘**：79GB（使用 47%，剩余 41GB）✅
- **系统负载**：0.0（非常空闲）✅

**结论**：✅ 资源充足，可以部署新项目

---

## ✅ 已安装软件

- **Docker**：v29.1.3 ✅
- **Git**：v2.34.1 ✅
- **Nginx**：运行中 ✅

**结论**：✅ 所需软件都已安装

---

## 📊 端口占用情况

### 已占用端口
- **3000**：sweetyshell-backend（旧项目）
- **5432**：PostgreSQL（旧项目）
- **3002**：sweetyshell-map

### 新项目需要的端口
- **3100**：Backend（新）- ✅ 可用
- **8101**：Worker（新）- ✅ 可用
- **5433**：PostgreSQL（新）- ✅ 可用

**结论**：✅ 新项目所需端口都可用，不会冲突

---

## 🐳 Docker 容器状态

### 运行中的容器（4 个）
1. **sweetyshell-map**（新上线 2 小时前）
   - 端口：3002
   - 状态：健康运行
   
2. **sweetyshell-backend**（3 天前）
   - 端口：3000
   - 状态：运行中
   
3. **sweetyshell-redis**（2 个月前）
   - 状态：健康运行
   
4. **sweetyshell-postgres**（3 个月前）
   - 端口：5432
   - 状态：健康运行

### 已停止的容器（1 个）
5. **musing_pike**（3 个月前停止）
   - 状态：已退出

**结论**：✅ 旧项目正常运行，可以独立部署新项目

---

## 📁 项目目录结构

```
/data/
├── docker/              # Docker 相关
├── sweetyshell/         # 旧项目主目录
├── sweetyshell-map/     # 地图项目（新）
└── lost+found/
```

**建议新项目路径**：`/data/video-flow`

---

## 🔧 部署建议

### 方案：独立部署（推荐）✅

**优点**：
- ✅ 不影响现有 sweetyshell 项目
- ✅ 端口完全独立（3100, 8101, 5433）
- ✅ 数据库独立（新容器）
- ✅ 可以并行运行，逐步迁移
- ✅ 回滚简单

**部署步骤**：
```bash
# 1. 克隆代码到服务器
cd /data
git clone <仓库地址> video-flow
cd video-flow

# 2. 配置环境变量
cp .env.example .env
vim .env

# 3. 一键部署
./scripts/deploy.sh

# 4. 验证
curl http://localhost:3100/api/tasks
curl http://localhost:8101/health
```

---

## ⚠️ 注意事项

### 1. 系统更新
- 有 38 个待更新的包
- 系统需要重启（*** System restart required ***）
- **建议**：部署前或部署后找时间更新

### 2. Nginx 配置
- 如果需要外网访问，需要配置 Nginx 反向代理
- 当前 Nginx 运行中，可以复用

### 3. 防火墙
- 检查是否需要开放端口（3100, 8101）

---

## 📋 部署前准备清单

### 必须配置
- [x] Docker 已安装
- [x] Git 已安装
- [x] 端口可用（3100, 8101, 5433）
- [ ] 设置数据库密码
- [ ] 确认火山引擎 API Key
- [ ] 确认阿里云 OSS 配置

### 可选配置
- [ ] Nginx 反向代理（外网访问）
- [ ] 域名绑定
- [ ] SSL 证书

---

## 🎯 下一步行动

### 立即可以执行（预计 30 分钟）

1. **推送代码到 Git 仓库**
   ```bash
   # 本地操作
   cd /Users/steven/works/20260909video_flow
   git init
   git add .
   git commit -m "MVP: video flow system"
   git remote add origin <仓库地址>
   git push -u origin main
   ```

2. **服务器部署**
   ```bash
   # 服务器操作
   ssh root@8.140.49.56
   cd /data
   git clone <仓库地址> video-flow
   cd video-flow
   cp .env.example .env
   vim .env  # 填写配置
   ./scripts/deploy.sh
   ```

3. **验证测试**
   ```bash
   # 创建测试任务
   curl -X POST http://localhost:3100/api/tasks \
     -H "Content-Type: application/json" \
     -d '{"createdBy":"测试","prompt":"测试任务"}'
   
   # 查看 Worker 日志
   docker compose logs video-worker -f
   ```

---

## 💰 成本预估

### 服务器资源
- **当前使用**：CPU 0.0, 内存 25%, 磁盘 47%
- **新项目增加**：CPU +10%, 内存 +15%, 磁盘 +2GB
- **预估总使用**：CPU 10%, 内存 40%, 磁盘 49%

**结论**：✅ 资源充足，无需扩容

### API 调用成本
- 火山引擎图生视频：¥0.5-2/次
- 阿里云 OSS：¥0.12/GB/月（存储）+ ¥0.50/GB（流量）

---

## ✅ 总结

**服务器状态**：✅ 优秀
- 资源充足（内存 12GB 可用，磁盘 41GB 可用）
- 所需软件都已安装（Docker, Git, Nginx）
- 端口无冲突（3100, 8101, 5433 都可用）
- 旧项目运行正常（不受影响）

**部署难度**：⭐⭐☆☆☆（简单）
- 代码已完成
- 环境已就绪
- 只需配置和启动

**预计时间**：30 分钟
- 推送代码：5 分钟
- 服务器部署：15 分钟
- 测试验证：10 分钟

**风险评估**：✅ 低风险
- 独立部署，不影响旧项目
- 端口隔离，互不干扰
- 回滚简单（停止容器即可）

---

**推荐：立即开始部署！** 🚀
