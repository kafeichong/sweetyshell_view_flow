[pytest]
testpaths = tests
markers =
    live_contract: 需要真实 Backend/DB/Fake Provider 的跨包用例，只由 scripts/run_mvp_contract.sh 运行
# 默认单元测试不跑跨包用例：它需要外部服务，误跑会把"没环境"变成"测试绿"。
addopts = -m "not live_contract"
