# 忽略文件
node_modules/
dist/
.env
.DS_Store
*.log
pnpm-lock.yaml
package-lock.json

# Python
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/

# 本地联调凭证，禁止提交
.preview-actor-token
*-actor-token

# Docker
.docker/
# 本地 compose 覆盖层。`docker compose` 会**自动加载**它，所以提交上去会被 pull 到生产，
# 把本该只在本机生效的东西（如 postgres 的 127.0.0.1:5433 端口映射）一并带过去。
docker-compose.override.yml
docker-compose.override.yaml

# IDE
.vscode/
.idea/

# 临时文件
*.tmp
*.swp

# 测试和开发临时文件
.playwright-mcp/
.test-token
scripts/test_*.sh
scripts/create_test_*.ts

# 交付包里随带的 ffprobe 二进制（各架构约 25–35MB）。**不进仓库**——太大，而且它只是
# 交付物的一部分；打包时由 scripts 之外的手工步骤放进 `packages/comfyui-video-flow-client/ffprobe/`。
packages/comfyui-video-flow-client/ffprobe/
